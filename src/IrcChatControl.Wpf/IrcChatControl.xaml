<UserControl x:Class="IrcChatWpf.IrcChatControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Focusable="True"
             UseLayoutRounding="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- Child receives the native swapchain HWND host (set in code-behind);
             the child window is hit-test transparent so mouse events reach WPF.
             The background matches the renderer's default clear color and is
             kept in sync by the Background property callback. -->
        <Border x:Name="ImageHost"
                Grid.Column="0"
                BorderBrush="Gray"
                BorderThickness="1"
                Background="#141414"
                ClipToBounds="True"
                SizeChanged="OnSizeChanged" />

        <ScrollBar x:Name="ChatScrollBar"
                   Grid.Column="1"
                   Orientation="Vertical"
                   Minimum="0"
                   Scroll="OnScroll" />
    </Grid>
</UserControl>
